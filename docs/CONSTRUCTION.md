# Deterministic construction fixture

Generated by `node scripts/construction-proof.mjs` (Node 24+), using the current main compiler. No AI provider is called.

## Semantic input

```json
{
  "objects": [
    {
      "type": "point",
      "name": "A",
      "x": 1,
      "y": 3
    },
    {
      "type": "point",
      "name": "B",
      "x": 0,
      "y": 0
    },
    {
      "type": "point",
      "name": "C",
      "x": 5,
      "y": 0
    },
    {
      "type": "polygon",
      "name": "ABC",
      "points": [
        "A",
        "B",
        "C"
      ]
    },
    {
      "type": "altitudeFoot",
      "name": "D",
      "from": "A",
      "to": [
        "B",
        "C"
      ]
    },
    {
      "type": "orthocenter",
      "name": "H",
      "triangle": [
        "A",
        "B",
        "C"
      ]
    }
  ]
}
```

## Actual compiler output

```text
A = (1, 3)
B = (0, 0)
C = (5, 0)
ABC = Polygon(A, B, C)
sAB = Segment(A, B)
auxAB = Line(A, B)
sBC = Segment(B, C)
auxBC = Line(B, C)
sCA = Segment(C, A)
auxCA = Line(C, A)
auxAD = PerpendicularLine(A, auxBC)
D = Intersect(auxAD, auxBC)
sAD = Segment(A, D)
auxAltB = PerpendicularLine(B, auxCA)
auxAltC = PerpendicularLine(C, auxAB)
H = Intersect(auxAltB, auxAltC)
```

To view it in GeoGebra, serve this repository locally and open `docs/construction-demo.html`. The applet requires network access to GeoGebra. The fixture is not evidence of live natural-language generation.
